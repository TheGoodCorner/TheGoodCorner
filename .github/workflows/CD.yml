names: CD_Action

on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    needs: ci-build
  steps:
    -name: checkout-code
    uses: actions/checkout@v6

    -name: Set up NodeJS
    uses: actions/setup-node@v6

    -name: Install Dep & build
    runs: |
      npm ci
      npm run build --if-present

    -name: Deploy APP
    run: |
    echo "execute deployment command here"
    # env
